meta {
  name: Destroy VM
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/vm/{{sessionId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{authToken}}
}

assert {
  res.status: eq 200
}
